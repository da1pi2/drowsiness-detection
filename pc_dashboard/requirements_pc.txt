# PC Server Requirements
# These libraries are for the PC (Windows/Linux/Mac)

# ===================== COMMON (pc server version) =====================
# Keep NumPy 1.x for stability with current OpenCV
numpy<2.0.0
# Standard OpenCV
opencv-python
# Compute euclidean distances
scipy>=1.7.0
# Web Dashboard
streamlit

# ===================== DLIB VERSION =====================
# dlib is compiled more easily on PC with more RAM
# Requires CMake and VS Build Tools on Windows
dlib>=19.24.0

# ===================== MEDIAPIPE VERSION =====================
# MediaPipe is easier to install (no compilation needed)
# Used in: pc_server_mediapipe.py, streamlit_dashboard_mediapipe.py
mediapipe